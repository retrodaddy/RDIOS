> **ARUMBU is the product name introduced after this document was written.** Everything below was written under, and refers throughout to, the internal engineering name "RDIOS." That reasoning is preserved exactly as frozen — nothing in this document has been altered or renamed. "RDIOS" remains the correct internal/engineering term for the underlying platform this document describes; "ARUMBU" is what that platform is now called on every customer-facing screen.

Status: 🟡 Living document — updated as the project moves, never frozen. This is the operational source of truth; the philosophy/architecture layer below is what it operates under, not what it contains. Open this before starting work each day.

# RDIOS Master Roadmap v1

## 1. Current Project Status — stated plainly

**Frozen (constitutional — govern every decision below, not reopened without real implementation evidence proving a flaw):**

Product Foundation v1 · Product Philosophy v1 · Experience Principles v1 · Institution Setup Experience v2 (v1 + its Reconsideration superseded/folded in) · Visual Design System v1 · People Domain Review v1 · Audit Engine Design v1 · Architecture Freeze Declaration v1 · Platform Integration Strategy v1 · Institution Intelligence Principles v1 · **Governance & Responsibility Model v1** · **Institutional Policy Model v1**.

**Constitutional Phase: Complete.** Declared by the founder. Every document listed above is now the whole of RDIOS's constitution — Governance & Responsibility Model v1 (who decides, and how authority moves — Areas of Responsibility, delegation/temporary/escalation/emergency as one time-boxed-widening primitive, Approval Chains named by Area not by person) and Institutional Policy Model v1 (what a correct decision looks like — Policy as the institution-owned statement Business Rules are compiled from, interpreted but never authored by Tamizhi) close the constitutional layer that began with Architecture Freeze v2. No further philosophy, constitutional, or foundational-review documents get written unless implementation itself discovers a genuine flaw — the Constitution is extended by implementation from here forward, never redesigned by it.

**Implementation Phase: Active.** Every session from here runs Build → Test → Verify → Polish → Ship against this roadmap's next milestone, with the same six-step Verification Policy (§5) already proven across M2 through M5, no exceptions for size or urgency.

**Built and live-verified (real code, running, click-tested this engagement):**

- Project scaffold — Next.js 14 App Router, Tailwind, TypeScript, at `G:\RDIOS`. `typecheck`/`lint`/`build` all clean.
- Identity & Tenant foundation — Person, Institution, Institution Membership types; a swappable `IdentityProvider` interface; today backed by an **in-memory mock provider only**, explicitly not durable, explicitly temporary per standing instruction not to provision Supabase yet.
- Dev-mode auth — institution creation (onboarding), email-based dev-mode login, invite, accept-invite. All four flows live-clicked end to end, including a real bug found and fixed live (Act Now's "only one here" card was checking the wrong membership count).
- Application shell — sidebar with all nine destinations, minimal top bar.
- Home — Act Now / Be Aware / History, live-verified with real narrated History entries across create/invite/accept.
- **M2 — Institution Setup Experience v2, implemented and verified.** Purpose captured as one optional sentence at onboarding, never required, rendered permanently in Be Aware. A second Act Now contributor, "Who reports to whom?", originally a minimal free-text org-shape capture opening inline on Home — **superseded by the coherence pass below** once People (M3) gave it real Position data to point to instead.
- **M3 — People, implemented and verified.** Position/Affiliation/Capability implemented per the frozen People Domain Review, behind a swappable `PeopleProvider` (mirroring `IdentityProvider`'s exact pattern), backed by an in-memory mock. A real roster screen (`/people`) listing every active member with their current position, and every position with its current holder. A real profile screen (`/people/[personId]`) — appoint/end a Position holder (single-parent `reportsToPositionId` only, multi-parent deferred to M4), add/end Affiliations (append-only), grant/revoke Capabilities (not append-only, per the frozen Capability Domain Reconsideration), and Atomic Offboarding (ends every active Position holding and Affiliation for that person in that institution in one action, behind a confirmation dialog since it's the one action on the screen that reads as final). Appointing to an already-filled Position correctly closes the existing holder before inserting the new one — live-verified by re-appointing the same Position and confirming the prior holder shows "Ended," not a silent overwrite. History narrates appointments and offboarding, matching M2's discipline. Live-verified with one real Position, one Affiliation, and one Capability all created, ended/revoked, and (for Position) re-appointed independently, plus a full regression pass across Home/Settings.
- **Coherence pass, implemented and verified (post-M3, pre-M4, no milestone number — a refinement, not new scope).** Triggered by a live first-time-founder review that judged RDIOS purely on whether it felt like one institution rather than several connected applications, and found four real cracks: Home's free-text org-shape card had gone stale the moment People became real, still asking a question People had already answered; invited members were invisible in People even though they were real in Settings; the nav rail and every placeholder defaulted to corporate vocabulary regardless of the institution type the founder had just chosen; and two sentences of internal architecture language ("frozen Institution Configuration Layer," "Dev-mode sign-in ... infrastructure") had leaked into founder-facing copy. Fixed all four: `os/attention/org-shape-store.ts`, its actions, and `ShapeOrganizationCard.tsx` are deleted — Home's Act Now now checks real Positions and links straight to `/people`, and Be Aware's "Organization" line summarizes real position/holder counts instead of a parallel free-text description. People's roster now lists invited memberships alongside active ones with an "Invited" badge. `os/institution/terminology.ts` (new, pure data) resolves nav labels/questions and Position/Affiliation/Capability/institution-name placeholder examples per institution type — a temple sees "Community" in the nav and "Head Priest" as a Position example, a hospital sees "Patients" and "Chief of Surgery", with Company/Other keeping the original neutral defaults. The Shell sidebar now shows the signed-in person's current Position title under their name — a real, honest "role" signal built from existing People data, deliberately stopping short of gating navigation by role since Authority & Permissions (M5) doesn't exist yet and a placeholder permission system would be new architecture the founder explicitly ruled out. History's "appointed someone" / "offboarded someone" bug fixed to use the real person's name (or "themselves" when self-appointed). Live-verified end to end as a second Temple institution (Sri Meenakshi Temple Trust): dynamic onboarding placeholder, "Community" nav label, "Head Priest" position placeholder, invited member visible immediately in People, Home's Act Now/Be Aware tracking real Position data with zero duplication, sidebar role display, and correct History narration — plus a regression pass on Money/Work/Settings/Login.
- **M4 — Organization Builder, implemented and verified.** The flagship visual experience named in the Product Foundation, built for real: a canvas at `/people/organization` (linked from the People roster) rendering every Position as a draggable node, connected by SVG lines to each of its parents. `Position.reportsToPositionId` (single) generalized to `reportsToPositionIds: string[]` (multi-parent) plus `canvasX`/`canvasY` (persisted layout) and `description` — the schema change the M3 single-parent placeholder always anticipated. Drag a node to move it (persisted). Drag from the small handle on top of a node to another node to connect it as a parent — drag the same pair again to disconnect; the release always writes the complete new parent set in one atomic call, never a partial add/remove. Click empty canvas to create a Position exactly where clicked, no form. Click any node for a side panel that slides in with the canvas still visible behind it, per the Product Foundation's own description of this experience — editable name/description, holder appoint/end (reusing M3's `appointHolderAction`/`endHolderAction` unchanged), the full "reports to" list with per-parent removal, and a read-only "direct reports" list computed from the same data in the other direction. Live-verified end to end: three Positions created by clicking the canvas (no form), one connected to two separate parents by dragging its handle twice — genuine multi-parent, the exact completion criteria the roadmap named — confirmed from both directions (the child's side panel lists both parents, each parent's side panel lists the child under "Direct reports"), confirmed to persist across a full page reload, and confirmed to disconnect correctly when the same drag is repeated. Home's Be Aware and the People roster both picked up the multi-parent structure with zero additional wiring, since both already read through the same `PeopleProvider` this milestone extended rather than replaced.
- **Cycle prevention (pre-M5 hardening, no milestone number).** The founder's own instruction before starting M5: the multi-parent graph M4 introduced could be dragged into a cycle one connection at a time, something the old single-parent tree structurally couldn't do. `mockPeopleProvider.updatePositionParents` now walks the proposed parent's existing ancestor chain before committing and rejects (`ok: false`) anything that would make a Position its own ancestor. The canvas reverts the optimistic connection and shows a calm, transient notice — never a raw error, never blocking the rest of the canvas. Live-verified: connected A→B, then attempted B→A — rejected, notice shown, connection count unchanged.
- **M5 — Authority & Permissions, implemented and verified, reframed by the founder as a product milestone, not a security milestone.** The explicit brief: the goal is not to hide pages, it's to make every person feel RDIOS understands their real role — permissions support responsibility, they don't expose access-control mechanics. `engines/authority/` is the first Shared Engine Layer piece (Product Foundation §7's "Authorization's mechanism ... reusable from RDE," now real): a small catalog of three real responsibilities (`members.invite`, `organization.manage`, `people.offboard` — deliberately not abstract "roles"), and a resolver returning the union of whatever responsibilities a person's currently-active Position holdings carry, resolved once per request onto `IdentityContext.permissions` (the "role grants ∪ Position grants, resolved once" pattern named reusable from RDE). Authority lives entirely on the real M4 organization graph — a Position now carries a `responsibilities: PermissionKey[]` field, editable only by the institution's founder (a new `Institution.founderPersonId`, set once at creation, who always holds every responsibility — the bootstrap every real institution needs). Every mutating People/Settings action checks `ctx.permissions` before touching anything, server-side, non-negotiably. The UI half of the brief: no control disappears — every button a person can't currently use stays visible, disabled, with a plain-language reason ("Offboarding isn't your responsibility here," never "permission denied" or "insufficient role"); nav destinations and full pages are never hidden, only the specific actions within them. Live-verified with two real people in one institution: the founder granted a Position ("Operations Lead") both `members.invite` and `organization.manage` but not `people.offboard`, appointed a second person to it, and confirmed — signed in as that second person — the sidebar showed their real title, Settings' Invite worked, creating/connecting Positions and appointing/ending holders worked, and Offboard stayed disabled with the correct explanation, while every page remained fully visible throughout. A third, position-less person was confirmed to see every page with every action disabled and explained, never a missing page.
- **Constitutional Phase (Governance & Responsibility Model v1, Institutional Policy Model v1) — design-only, frozen.** See §1's own header above for what these two documents settle. No code from this phase; M6 is the first place their content becomes real.
- **M6 — Work, implemented and verified.** The first Shared-Engine-Layer-backed application, and the first real proof that Governance & Responsibility Model v1 is buildable, not just prose. Two work item kinds — Task (`open → in_progress → complete`, manual assignment) and Approval (`pending → approved/rejected`, driven by a **real Approval Chain**) — both seeded from a fixed `task.default`/`approval.default` template shape, per Product Foundation §7's own named starter set; no template editor built, the same "smallest implementation" discipline every milestone has used. An Approval's chain names **Areas of Responsibility (`PermissionKey`) in sequence, never people** — exactly Governance §5 — resolved fresh every time against whoever currently holds that Area (`engines/authority/resolver.ts` gained `listPositionsWithResponsibility`, `personCanSatisfyArea`, `personHoldsPosition`, `findEscalationTarget`, alongside the existing `resolvePermissions`). A fourth catalog entry, `work.manage`, was added to `engines/authority/types.ts` — the catalog growing exactly the way Governance §11's own Backlog note anticipated, with zero UI code changes needed since `PositionSidePanel`'s "Responsible for" section already iterates the whole `PERMISSIONS` array generically. **Same-actor exclusion** (Governance §6) is real and permanent: an Approval's creator can never decide any of its own steps, full stop — verified live, rejected with "You requested this approval — you can't also decide it." **Escalation** (Governance §7, the "Escalate" verb the Architecture Freeze Declaration named as expected but unbuilt) is real: any stuck step can be escalated by its creator or anyone holding `work.manage`, which finds the first Position carrying that Area and widens the step's pool to whoever holds that Position's parent one hop up the real M4 org graph — never reassigning the step, only adding a second valid path to satisfy it. Work's own Attention Contract is now the first genuinely per-person contribution to Home's Act Now: tasks assigned to *you*, and Approval steps *you* can currently satisfy (accounting for escalation), computed the same way `composeActNow` already worked for everything else — the literal proof that M5's Authority Engine and the Attention Engine compose correctly together. Comments (a verb RDE already proved, per the Architecture Freeze Declaration) are real on every work item. Live-verified end to end with three real people: a Task created and assigned by the founder, completed by its assignee; a single-step Approval requiring a deliberately **vacant** Position's Area, confirmed stuck (the founder, as creator, correctly blocked from approving her own request), escalated by the founder to the vacant seat's parent Position, and then approved by that parent Position's real holder — appearing in her Home Act Now the instant escalation made her eligible, with History narrating every step (request, escalation, approval) in plain sentences. Full regression pass on People/Organization confirmed unaffected.
- **ARUMBU Branding Migration (post-M6, pre-M7, no milestone number).** RDIOS renamed to ARUMBU across every customer-facing surface — login, onboarding, Shell, metadata, favicon, error copy, Settings — via a single swappable `BrandMark`/`BrandLockup` component and an `app/icon.svg` placeholder. Internal engineering references (comments, `package.json`, the repo folder name) deliberately left as "RDIOS," per the founder's own explicit instruction. All 19 pre-migration documents in `docs/` preserved unmodified with a prepended historical note, never renamed. Full account in `ARUMBU Branding Migration Report.md`.
- **M7 — Finance & Assets, implemented and verified.** The first Institution Application built on the frozen platform rather than proving a platform mechanism. `applications/finance/` — Expense and Income converge on one shared transaction spine (`FinanceTransaction = Expense | Income`, mirroring Work's `Task | Approval` union exactly), Financial Accounts as a lightweight institution-neutral bucket (Cash/Bank/Income/Expense/Asset/Liability/Equity), and the Asset Registry (name/category/status/custodian/warranty/service notes), all institution-neutral by construction — no company-accounting assumptions baked in. Three new catalog entries — `finance.manage`, `treasury.approve`, `assets.manage` — required zero UI changes, confirming Governance §11's growth model a second time. **Same-actor exclusion** extended to Finance independently of Work's own copy: an expense's recorder can never approve it. Attention gained three real, non-artificial contributions (a decidable pending expense, an asset with no custodian, an expiring warranty); History gained nine new call sites, all following the existing plain-sentence discipline. A named Policy extension seam (`applications/finance/policy.ts`) stands in for Institutional Policy Model v1 until a real Policy Engine exists. Live-verified end to end across a Temple institution with two real people (a real Treasury approval decided by someone other than the expense's creator) and spot-checked on a Hospital institution. Full account in `ARUMBU M7 Finance & Assets Report.md`.
- **ARUMBU Platform Cohesion Review v1 (post-M7, no milestone number, pure observation).** Live use of ARUMBU across five institution types, checking whether seven real applications felt like one operating system rather than seven good ones. Found real strengths (institution-neutrality genuinely held; the M7 approval mechanics were proven live, not just read) and real, honestly-named debt: four of eight nav destinations still stub screens; no shared form-input primitive (the same ~89-character class string hand-typed 28+ times); `notResponsible()` reimplemented identically three times; History recording creation/granting far more reliably than ending/revoking. Fed directly into the Community Domain Reconsideration and this milestone's own History discipline. No code changed to produce it.
- **ARUMBU Community Domain Review v1 + Reconsideration v1 (post-Cohesion-Review, design-only).** Defined Community as People's external counterpart — Contact + Relationship, three universal Directions (Receiving/Supporting/Supplying), Type as institution configuration — then challenged it directly: confirmed the three Directions against a fourth "Partner" candidate (rejected — decomposes into concurrent existing Directions or a Type-level status label), added Organization as a first-class Contact kind alongside Individual (confirmed twice, independently, by direct examination and by forward-compatibility against Procurement/Reports), and answered the six-month test with "a Contact's detail view should read as its own filtered History," not a static fact sheet. No code.
- **ARUMBU Universal Record Model v1 (post-Community-review, design-only, constitutional-altitude).** Investigated whether one "Record" concept sits beneath Person/Contact/Task/Asset/Policy — found real, convergent evidence (Audit's `subject_type`/`subject_id`, Policy's own owner/lifecycle/hierarchy language, Governance's Delegation, all independently reaching the same shape) — concluded yes, but only as a discipline and checklist layered beside the Application Layer's existing "record surface" requirement (Product Foundation §4), never a literal shared table or base class. No code.
- **ARUMBU Constitutional Index v1 (post-Universal-Record-Model, pure index).** Every document in `docs/` catalogued by purpose, dependency, and which milestone it governs, with a task-oriented reading list. Surfaced one honest finding: Experience Principles v1 is referenced as frozen everywhere but has no standalone file in this repository — its content is embedded inside Product Foundation §5.
- **M8 — Community, implemented and verified.** Built exactly per the accepted Community Domain Review v1 and its Reconsideration, following the identical implementation discipline People/Work/Finance already established — no new architecture pattern introduced. `applications/community/` — Contact (`kind: "individual" | "organization"`, never collapsed into one shape), Relationship (the three accepted universal Directions, free-text institution-configured Type, `lastActivityAt` as the honest, non-artificial basis for staleness — never a manufactured health score), an embedded, lightweight `PointOfContact` list for Organization contacts per the Reconsideration's own scoping note, and the same `DocumentRef` placeholder pattern Finance already proved. One new catalog entry, `community.manage` — confirmed live in `PositionSidePanel`'s responsibility list with zero code changes needed there, a fourth independent proof of Governance §11's growth model. Attention gained two real contributions (a contact with no way to be reached; a relationship quiet past 180 days) — no artificial reminders, matching Finance's own restraint. History records creation, edits, relationship-added, and relationship-ended with equal weight, deliberately closing the creation/ending asymmetry the Cohesion Review found elsewhere. `os/attention/history-store.ts` gained an optional `subjectType`/`subjectId` on `HistoryEntry` (additive, backward-compatible, no existing call site touched) plus `listHistoryForSubject` — the first real realization of the Audit Engine Design's own frozen `subject_type`/`subject_id` shape, letting a Contact's detail drawer render a genuine filtered Timeline instead of a static fact sheet, exactly the Reconsideration's six-month-test answer. Live-verified across Company, Temple, and NGO institutions: an Individual and an unreachable Organization contact created, the unreachable nudge confirmed live in Act Now and confirmed to clear the moment a point of contact was added, a relationship ended, and `community.manage` confirmed in the Position responsibility panel. Full regression pass on Work and Money confirmed clean, zero console errors. Full account in `ARUMBU M8 Community Report.md`.
- **Platform Integration Sprint 3, implemented and verified.** Not a new milestone — an implementation and quality pass across the seven built applications, explicitly requested before M9 (Projects), since Projects will connect People/Community/Work/Finance and would amplify, not fix, any inconsistency between them. The Universal Timeline (Community's own, from M8) extended to People's Positions, Work's Tasks/Approvals, and Finance's Transactions/Assets — the same `subjectType`/`subjectId` mechanism, no new engine, every application now reading its own filtered slice of History. A real bug found and fixed during this extension: a Timeline's fetch only ran on mount, so an action taken without closing and reopening the drawer (appointing someone, editing a contact) never appeared until a full reload — fixed by refetching alongside every `onChanged()`/`router.refresh()` call, across all four Timeline implementations including Community's own, live-verified afterward with a three-event Position Timeline (created → appointed → ended) updating correctly with zero page reloads. Every previously-silent People lifecycle transition now writes History with equal weight to its creating counterpart — ending a Position holding, adding and ending an Affiliation, granting and revoking a Capability, reopening a completed Task, editing an Asset's service notes and a Position's description — closing the exact creation/ending asymmetry the Platform Cohesion Review named. `Position` gained `createdByPersonId` (Provenance, per the Universal Record Model's own checklist) and three new provider lookups (`getPositionHolder`, `getAffiliation`, `getCapability`) needed to narrate an ending honestly. A "Related records" placeholder section (Community's own pattern) added to Positions, Work Items, Transactions, and Assets — calm, one sentence, no cross-record linking built. Two real z-index drift bugs found and fixed (`CreatePositionCard`/`AppointHolderCard` at `z-[80]` against every other create drawer's `z-[70]`; `PositionSidePanel` at `z-[70]` against every other detail drawer's `z-[75]`) — the Platform Cohesion Review had already named this drift; this sprint closed it. Attention, Search-readiness, and visual consistency were reviewed and found already disciplined — no artificial Act Now items existed anywhere, and no code changes were needed there. Mobile confirmed regression-free at 375px, including the newly-added Timeline/Related Records sections. Full account in `ARUMBU Platform Integration Sprint 3 Report.md`.
- **M9 — Projects, implemented and verified.** The coordination layer, built exactly per the founder's own brief: a Project is the container an institution already understands as "how we accomplish something meaningful," never a task list or board. `applications/projects/` — Identity (name/description/purpose/status/priority/owner/start-target-completed dates/stage/health), Members (Owner as its own dedicated field, Members and Observers as a role-tagged list), Stage as free text with a suggested default sequence (Planning/Approved/In Progress/Blocked/Completed/Archived) — the same "institution-configured, not a hardcoded enum" reasoning Expense.category and Relationship.type already established, since the brief explicitly asked for this to stay configurable without a Policy Engine being built to do it. Health is manually set (on_track/at_risk/off_track), never computed — the same restraint the Community Domain Reconsideration used to reject an automatic relationship health score. One new catalog entry, `projects.manage`, gating Create/Archive/Change-stage/Manage-members/Close — the fifth independent proof of Governance §11's growth model. **Real convergence, not placeholders**: Work Items (Task/Approval), Finance Transactions and Assets, and Community Contacts each gained an optional `projectId`, plus a thin `setXProjectAction` in their own `actions.ts` to attach/detach an existing record — Projects organizes these applications' own records without duplicating any of their create/manage flows, exactly the brief's "Projects do not duplicate Work. They organize Work." The Project detail drawer's "What's happening" section makes this real and functional: attach/detach existing Work/Money/Assets/Community records from a Project, live. Every meaningful transition is narrated — created, stage changed, member added/removed, owner changed, health changed, completed, archived — closing the same creation/ending asymmetry every prior milestone has had to close. Projects gained the same Timeline (`listHistoryForSubject`, no new mechanism) and a "Related records" placeholder for what isn't yet real (Documents, Governance beyond membership) — reusing Community's exact pattern. Attention gained three real, non-artificial contributions — a Project stuck on Blocked, a Project past its own target date, a Project with no owner named — surfaced only to whoever can act on it (Projects' own Area, the Project's owner, or one of its members), plus a Be Aware summary. **A real bug found and fixed live**: the mock providers mutate their stored objects in place and return that same reference, so code that read a "before" field *after* calling the mutating provider function was actually reading the *already-mutated* value — Project's own stage-change narration hit this first ("moved from Blocked to Blocked"), and the same root cause was found to silently disable Work's M6-era "reopened a task" narration too (the `else if (item.status === "complete")` check always read the post-mutation status). Both fixed by capturing the "before" value into a local constant before the provider call. Live-verified end to end on a Company institution: a Project created with a real owner, a Task created on Work and attached to it via the drawer's "Attach existing" picker, the Attention "Blocked" nudge appearing in Act Now and disappearing the instant the stage changed (confirmed with corrected History narration afterward), Be Aware's Projects summary tracking live, and the whole page confirmed overflow-free at 375px mobile width. Full account in `ARUMBU M9 Projects Report.md`.
- **M10 — Documents, implemented and verified.** ARUMBU's institutional memory, built exactly per the founder's own brief: a Document represents institutional knowledge — a Policy, a Contract, a Certificate — never a file itself; a file is only one possible attachment. `applications/documents/` — Identity (title/type/description/status/owner/document number/expiry), `type` free text with the brief's own 22-item list as datalist suggestions (the same "institution-configured, not a hardcoded enum" reasoning Project.stage and Expense.category already established), Attachments mocked exactly like every prior milestone's `DocumentRef` (a filename and kind a person records, no storage provider built), and Versions kept strictly append-only — "No Git. No branching. No merge conflicts," per the brief — Restore never rewrites or deletes, it appends a fresh version carrying the old one's content, narrated honestly as "restored from version N." Relationships reuse the Universal Record Model's own polymorphic `subjectType`/`subjectId` shape, one-directional (the Document names what it relates to — Person, Project, Money, Community, Work, Asset — never the reverse), so building this needed zero changes to any other application's schema. One new catalog entry, `documents.manage`, gating everything — the sixth independent proof of Governance §11's growth model. A minimal, real approval workflow (`approvalStatus: none/pending/approved/rejected`) was added since the brief's own "Document awaiting approval" Attention example requires a decidable state to exist; the same same-actor exclusion every Approval-shaped decision on the platform already enforces (a document's creator can never decide its own approval) was live-verified to correctly block the founder from approving their own submission. Attention gained two real, non-artificial contributions — a document deliberately submitted for approval, and a document whose `expiresAt` is approaching or past (doubling as both "expiring certificate" and "expiring contract," the brief's own two examples of the same underlying fact) — plus a Be Aware summary. "Mandatory document missing," the brief's third Attention example, was deliberately not built: it needs an institution-configured "required documents" concept that exists nowhere in the platform, and inventing one here would have been new architecture, not implementation — named honestly rather than faked. Timeline, Related Records (a placeholder for Policies and Search), and every visual primitive (`DataTable`, `Badge`, the create/detail drawer shape) are reused unchanged from every prior milestone — no Documents-specific UI language was invented. Live-verified end to end on a School institution (Sri Meenakshi School, confirming the nav correctly reads "Students" not "Customers"): a Certificate created with a real expiry date and a first version, linked to a Person, submitted for approval and correctly blocked from self-approval, a second version added, and version 1 restored as a new version 3 — all narrated correctly on the Timeline, the expiry nudge confirmed live on Home ("Expires in 11 days"), and the pending-approval nudge confirmed correctly *absent* for its own creator (the same same-actor discipline extended to Attention, not just the decision itself). Mobile confirmed overflow-free at 375px with the detail drawer open. Full account in `ARUMBU M10 Documents Report.md`.
- **M11 — Reporting & Analytics, implemented and verified.** Two deliberately separate engines, kept separate in code the way the founder's brief kept them separate in prose: Reports answer "what happened?", Analytics answers "what deserves attention?" `applications/reports/` — a `Report` is a frozen historical snapshot (`snapshot.ts` computes `ReportMetric[]`/`ReportChart[]` once, at generation time, reading the same providers every Be Aware line already reads) across the brief's own closed, universal set of nine categories (Institution/People/Organization/Work/Financial/Community/Project/Document/History Overview-or-Summary) — deliberately a real enum here, not the "suggested free-text vocabulary" pattern every prior domain used, since the brief explicitly said "no institution-specific reports, universal only." Filters (date range, person, project, status, type) are interpreted per category only where they honestly apply, reusing M9's `projectId` cross-references to narrow Work/Money/Documents/Community without a new join. Reports are genuinely historical — "do not regenerate automatically," per the brief — and genuinely disposable: `deleteReportAction` performs a real delete, the one deliberate departure from every other Record's archive-and-keep convention on this platform, since a Report is a summary of facts that live elsewhere, never the one place a fact is remembered. One new catalog entry, `reports.manage`, gates generate/save/delete; reading a Report and its Analytics stays open to everyone, matching how Be Aware's own aggregate rollups have always worked. **Analytics** (`applications/reports/analytics.ts`) computes `Observation[]` fresh on every read, never persisted — five flat-fact observations computable from live state alone (unfilled positions, approvals stuck over a week, documents expiring, relationships quiet 90+ days, projects blocked or overdue) plus one genuine trend observation, comparing the two most recently saved Financial Summary reports' expense totals to say whether expenses have risen or fallen — the one place this milestone gets real "rising/falling" analytics without inventing a time-series store, since Reports already are historical snapshots and comparing two of them honestly is a real trend, not a guess; skipped entirely with fewer than two to compare. Home gained exactly one new Act Now card (stuck approvals, aggregate not per-item, gated to `work.manage` holders) and one Be Aware line — "never spam," per the brief, honored by surfacing the single most actionable observation rather than all of them. Charts are deliberately minimal: every `ReportChart`, regardless of its declared kind, renders as one plain inline-SVG horizontal bar — "numbers first, charts second" was read as license to build one honest chart shape instead of four, named directly in the Technical Debt Register rather than silently simplified. Live-verified end to end on an NGO institution (Coastal Relief NGO, confirming "Beneficiaries" nav labeling): an Institution Overview report generated and opened showing a correct live rollup across every domain; two Financial Summary reports generated around a new ₹20,000 expense, correctly producing "Expenses have risen from ₹5,000 to ₹25,000 since your last Financial Summary report" in both Analytics and Home's Be Aware; a report renamed (narrated "saved changes") and deleted with an inline two-step confirmation (narrated "deleted"), after which the trend observation correctly disappeared since fewer than two Financial Summary reports remained — proving the comparison logic honestly both ways. Mobile confirmed overflow-free at 375px. Full account in `ARUMBU M11 Reporting & Analytics Report.md`.

**Prototype, not production-grade — true today, named honestly:**

Everything above is real and working, and none of it survives a server restart. There is no database. Identity, institutions, memberships, People (Positions/Affiliations/Capabilities), and history all live in process memory. This is by explicit design (the founder's instruction to stay infrastructure-independent until Supabase is provisioned), not an oversight — but it means nothing built so far should be mistaken for durable.

**Design-only — frozen on paper, zero code:**

Projects, Documents, Reports, the real Audit Engine (History is still keyed by a lightweight append-only store, not a real `audit_records` table with Events subscription — though M8 gave it a real `subjectType`/`subjectId` shape, the first concrete step toward the frozen design), and the deeper Institution Configuration Layer (per-type Organization Templates, full terminology coverage, branding, business rules). Institution Setup v2's "shape your organization" decision is now answered by real Position data instead of the free-text placeholder that used to sit ahead of it — the placeholder mechanism itself has been retired, not merely superseded on paper. The coherence pass's `os/institution/terminology.ts` is a first real slice of the Configuration Layer's terminology concept, covering nav labels and a handful of placeholders; full Organization Templates and the rest of the Configuration Layer's scope remain design-only. Tamizhi's *behavior* is now frozen (Institution Intelligence Principles v1); Tamizhi itself — the product on the other side of ARUMBU's two doors — remains entirely unbuilt, per the Platform Integration Strategy's own boundary. Governance & Responsibility Model v1's Approval Chains are now real (M6); Delegation, Temporary Authority's narration treatment, Emergency Governance, and Separation of Duties as a *configurable* (rather than permanent-default) constraint all remain named, unbuilt work for later milestones. Institutional Policy Model v1 is entirely unimplemented — no Policy record type exists yet; Work's Approval Chains and Finance's expense approvals today name raw Areas, not Policies (Finance names its own extension seam, `applications/finance/policy.ts`, but the seam is empty). The Universal Record Model v1 and Community Domain Review v1 (+ Reconsideration) are accepted design reasoning, not yet frozen — M8 implements the latter, but neither document has been formally folded into the Constitution the way the Institution Setup Reconsideration was folded into Setup v2.

**Not yet scoped anywhere:** Developer Platform, Marketplace, Public APIs. No frozen document names these. They appear in this roadmap because the founder named them; they get their own design pass when their milestone actually approaches, not before.

---

## 2. Product Layers

| Layer | Status | Depends on | Current phase | Next milestone | Build order | Unknowns |
|---|---|---|---|---|---|---|
| Platform Foundation | Prototype | — | Live, mock-backed | Real Supabase provider swap | Done (v1) | When Supabase gets provisioned is the founder's call, not a technical blocker |
| Identity | Prototype | Platform Foundation | Live, mock-backed | Same as above | Done (v1) | Multi-institution-membership UI not yet designed in detail |
| Institutions | Prototype | Identity | Live — name, type, Purpose, minimal org-shape | People (real Positions) | M3 | None |
| People | Prototype | Institutions | Live, mock-backed — Position/Affiliation/Capability, roster + profile screens, multi-parent + responsibilities since M4/M5 | Node deletion/archival | M9 | Membership-to-multiple-Positions question named open in the People Domain Review — still open |
| Organization (Builder) | Prototype | People | Live, mock-backed — canvas at `/people/organization`, drag-to-move, drag-to-connect, multi-parent, cycle-guarded, Responsible-for section | Node deletion/archival | M9 | None known yet |
| Authority & Permissions | Prototype | Organization | Live, mock-backed — `engines/authority/`, 7-key catalog (grew by one each for Work, Finance ×3, Community), resolved once onto `IdentityContext.permissions`, real gates on every mutating People/Settings/Work/Finance/Community action, plus Escalation and Approval-Chain resolution helpers | Real permission catalog entries for Projects/Documents/etc. as each gets built | M9 | The catalog has now proven it grows cleanly across four applications, as Governance §11 predicted — no sign yet that this stops scaling |
| Work | Prototype | Authority & Permissions | Live, mock-backed — Task and Approval, `task.default`/`approval.default` seeded templates, real Approval Chains (Governance §5), same-actor exclusion (§6), Escalation (§7), a genuinely per-person Attention Contract | Delegation, template editor, Round Robin assignment, Policy-backed chains | Done (M6) | Whether Approval Chains eventually need to name a Policy (Institutional Policy Model v1) instead of a bare Area is a real, named, deliberately deferred question |
| Money | Prototype | Work | Live, mock-backed — Financial Accounts, Expense/Income sharing one transaction spine, Asset Registry, same-actor exclusion, real Attention/History contributions, a named (empty) Policy seam | Real Policy content behind `applications/finance/policy.ts`'s seam | Done (M7) | Multi-currency, real ledger reconciliation, vendor management — all explicitly deferred in the M7 report |
| Customers (Community) | Prototype | Work | Live, mock-backed — Contact (Individual/Organization), Relationship (three universal Directions, institution-configured Type), Timeline via the new `subjectType`/`subjectId` History filter | Related-records wiring to Finance/Work/Projects once those seams are built | Done (M8) | Cross-institution Contact matching, a Vendor "Capability" equivalent — both named open in the Community Domain Reconsideration |
| Projects | Not Started | Work | Not designed | — | M9 | None yet |
| Documents | Not Started | Work | Not designed | — | M10 | None yet |
| Reports | Not Started | Money, Work | Not designed | — | M11 | None yet |
| Tamizhi | Not Started | Search, Attention Contract (both exist as concepts, not yet built as reusable surfaces) | Room reserved in Visual Design System only | — | M12 | Entirely unscoped beyond "don't need to redesign anything when it arrives" |
| Developer Platform | Not Started | Stable Attention Contract + Application Layer | Not named in any frozen document | — | Unscoped | Whether this becomes public-facing or internal-only is undecided |
| Marketplace | Not Started | Developer Platform | Not named in any frozen document | — | Unscoped | Same |
| Public APIs | Not Started | Authority & Permissions | Not named in any frozen document | — | Unscoped | Same |

---

## 3. Milestones

**One deliberate deviation from a strictly module-by-module order, named explicitly:** Institution Setup's implementation (M2) comes before People (M3), even though People is the larger, more foundational application. This isn't a reordering of the frozen layer list — it's honoring the founder's own explicit instruction from the session that produced Institution Setup Experience v2: *"it becomes the next thing we build before People."* Recorded here so the sequencing reads as intentional, not inconsistent.

### M1 — Platform Foundation
**Goal:** a clickable, verified skeleton — identity, tenant resolution, shell, Home — with zero real infrastructure required.
**Deliverables:** project scaffold, mock Identity provider, dev-mode auth, application shell, Home (Act Now/Be Aware/History).
**Completion criteria:** `typecheck`/`lint`/`build` clean; full flow live-clicked (onboard → invite → accept → sign out → sign back in).
**Preconditions:** none.
**Status: Done.**

### M2 — Institution Setup Experience
**Goal:** implement the frozen Setup v2 design exactly — Purpose captured as one optional sentence at institution creation, a "shape your organization" Act Now card offering a minimal starter structure.
**Deliverables:** Purpose field on Institution (mock provider first, real schema later); Purpose rendered permanently in Be Aware; a second Act Now contributor beyond Invite, offering the simplest possible Position-shaped suggestion (does not require the full Organizational Builder to exist).
**Completion criteria:** live-tested across at least two institution types (a company and a non-company, per the independence discipline already proven this engagement) showing genuinely different Act Now content without any hardcoded branching.
**Preconditions:** M1 done.
**Status: Done.** Purpose (`os/identity/types.ts`, captured in `OnboardingForm.tsx`) and org-shape (`os/attention/org-shape-store.ts`, `ShapeOrganizationCard.tsx`) both live, both wired into the Attention Engine, both verified across a School (Purpose stated, org shaped) and a Company (both skipped). Full verification pass (§5) completed — see the Technical Debt Register for the one real gap this milestone surfaced.

### M3 — People
**Goal:** the first complete application, the one that sets the quality bar for every application after it, per the founder's own standing instruction.
**Deliverables:** Person/Membership/Position/Affiliation/Capability implemented per the frozen People Domain Review; a real roster screen; a real profile screen; Offboarding implemented per the atomic-per-concept discipline named reusable from RDE.
**Completion criteria:** full verification pass (§5) plus a live walkthrough proving at least one Position, one Affiliation, and one Capability all working correctly and independently.
**Preconditions:** M2 done — People's roster meaningfully depends on institutions already having Purpose and at least a starter organizational shape to display.
**Status: Done.** `applications/people/` — types, a swappable `PeopleProvider` interface, an in-memory mock provider (`appointHolder` closes any existing active holder before appointing, `offboardPerson` atomically ends every active Position/Affiliation for that person in that institution). Server actions for appoint/end-position, add/end-affiliation, grant/revoke-capability, and offboard, each resolving identity first and recording History for the meaningful events. Roster screen (`/people`) and profile screen (`/people/[personId]`), both server components reading through the provider. Full verification pass (§5) completed — see the Technical Debt Register for the gaps this milestone surfaced.

### M4 — Organization Builder
**Goal:** the flagship visual experience named in the Product Foundation — canvas-based, drag-to-connect, multi-parent-capable from the schema up.
**Deliverables:** the interaction model already researched in the Product Foundation, built for real.
**Completion criteria:** a founder can visually lay out a real reporting structure, including at least one Position with multiple parents, without touching a form.
**Preconditions:** M3 done — Positions need to exist as real data before a builder can visualize them.
**Status: Done.** Canvas at `/people/organization` — draggable nodes, SVG connector lines, drag-from-handle-to-node to connect/disconnect, click-empty-canvas to create, click-node for a side panel (canvas stays visible behind it). `Position.reportsToPositionId` generalized to `reportsToPositionIds: string[]`; `canvasX`/`canvasY`/`description` added. Live-verified: three Positions created without a form, one connected to two separate parents by dragging twice (genuine multi-parent, confirmed from both directions and across a page reload), disconnect confirmed by repeating the same drag, node position confirmed to persist after reload. Full verification pass (§5) completed — see the Technical Debt Register for the one real gap this milestone surfaced (no cycle guard).

### M5 — Authority & Permissions
**Goal:** a real Authorization resolver — Position-derived permissions actually gating real actions, not just modeled as data.
**Deliverables:** the resolver mechanism named reusable from RDE (role grants ∪ Position grants, resolved once), a real permission catalog for RDIOS's own institution-configurable model.
**Completion criteria:** a Position-granted permission proven live to gate a real action, the same discipline already proven once in RDE.
**Preconditions:** M4 done.
**Status: Done.** Reframed by the founder mid-milestone as a product milestone, not a security one — the brief was to make every person feel RDIOS understands their real role, never to hide pages. `engines/authority/` (catalog + resolver), `Institution.founderPersonId` (bootstrap authority), `Position.responsibilities`, real gates on every mutating People/Settings action, and responsibility-framed UI (every control stays visible, disabled-with-a-plain-language-reason rather than removed) all live and verified. Live-verified with two real people holding genuinely different responsibilities in one institution — see the full account above §1. Pre-milestone hardening also completed at the founder's instruction: the M4 organization graph can no longer be dragged into a reporting cycle.

### M6 — Work
**Goal:** port the Work Item Engine's design — Task/Approval, template-driven workflow, Assignment — generalized for multi-tenancy.
**Preconditions:** M5 done — Work's Act Now items need real Authorization to gate who can approve what.
**Status: Done.** The Constitutional Phase's two final documents (Governance & Responsibility Model v1, Institutional Policy Model v1) were frozen immediately before this milestone began, and M6 is where their content first became real rather than prose. Task and Approval, both seeded from a fixed `task.default`/`approval.default` template (Product Foundation §7's own named starter set — no template editor built). An Approval's chain names Areas of Responsibility in sequence, never people (Governance §5), resolved fresh every time against `engines/authority/resolver.ts`'s new helpers. Same-actor exclusion (Governance §6) is a permanent default: an Approval's creator can never decide its own steps. Escalation (Governance §7 — the "Escalate" verb the Architecture Freeze Declaration named as expected but unbuilt) widens a stuck step one hop up the real M4 org graph, live-verified end to end: a single-step Approval against a deliberately vacant Position, correctly stuck, correctly blocked for its own creator, escalated to the vacant seat's parent, and approved by that parent's real holder — appearing in her Home Act Now the instant escalation made her eligible. Work's Attention Contract is the first genuinely per-person one on the platform. Full account above §1; verification pass (§5) completed in full, including regression on People/Organization.

### M7 — Money (Finance & Assets)
**Goal:** the smallest usable slice of institutional Finance — not an accounting application — built on the frozen platform.
**Preconditions:** M6 done.
**Status: Done.** Financial Accounts, Expense/Income (one shared transaction spine), Asset Registry, three new Areas of Responsibility, Attention/History/Policy-seam integration. Full account in `ARUMBU M7 Finance & Assets Report.md` and §1 above.

### M8 — Community
**Goal:** implement the Community domain exactly as defined by the accepted Community Domain Review v1 and its Reconsideration — Contact (Individual/Organization), Relationship (three universal Directions, institution-configured Type) — following the identical implementation discipline People/Work/Finance already established.
**Deliverables:** `applications/community/` (types, provider, mock provider, actions), one new Area of Responsibility (`community.manage`), real Attention/History integration, a Contact detail view with a genuine filtered Timeline.
**Completion criteria:** full verification pass (§5) plus a cross-institution walkthrough (at least Company, Temple, NGO) proving both Contact kinds, all three Directions, relationship ending, and the Attention/History contributions all work correctly.
**Preconditions:** M7 done — Community's `DocumentRef` and future Finance-linking patterns directly reuse what M7 already proved.
**Status: Done.** Full account in `ARUMBU M8 Community Report.md` and §1 above.

### M9 — Projects · M10 — Documents · M11 — Reports
**Goal, each:** the generalized version of the corresponding RDE application.
**Preconditions:** M6 done (all depend on Work existing as the shared decision substrate); Projects and Documents additionally benefit from Community's Contact/Relationship existing first, per the seams both M7's and M8's own reports name.

### M12 — Tamizhi
**Goal:** the assistant, entering exactly through the two homes already reserved for it — Search and the Attention Contract — per the Visual Design System's explicit test that nothing should need to change shape when it arrives.
**Preconditions:** a stable Application Layer (at minimum M6–M9) — an assistant recommending decisions needs real decisions to recommend.

### Unscoped — Developer Platform, Marketplace, Public APIs
No milestone number assigned. Each gets its own design pass, following the same discipline as everything else — Product Foundation-level thinking first, architecture second, implementation last — when the project is actually close enough for the question to be real rather than speculative.

---

## 4. Engineering Workflow

One lifecycle, every feature, no exceptions:

1. **Design** — what is this, in plain product terms, before any technical shape is proposed.
2. **Architecture Approval** — does it fit inside the frozen layers, or does it require reopening something? If the latter, it needs real implementation evidence of a flaw, not preference, per the Architecture Freeze Declaration.
3. **Implementation** — built, following Build Principles (§8).
4. **Testing** — typecheck, lint, build, at minimum.
5. **Verification** — live walkthrough, the discipline proven repeatedly this engagement to catch what static checks cannot (the Act Now membership-count bug, found only by clicking through as two real people).
6. **UX Review** — checked against the Experience Principles (tiering, calm, no forced interruption) and Institution Setup v2 where relevant.
7. **Performance Review** — does it stay fast under real institution-sized data, not just the two-person demo shape.
8. **Documentation** — this roadmap updated; any new technical debt named, not hidden.
9. **Freeze** — for architecture-level decisions only, not every feature; most features simply reach Done.
10. **Done** — meets the Definition of Done (§9) in full.

---

## 5. Verification Policy

No implementation is complete until all of the following pass, every time, no exceptions:

- **Type Check** — `tsc --noEmit`, clean.
- **Lint** — clean, zero suppressed warnings.
- **Production Build** — `next build` exits 0. This step alone has already caught a real deployment-breaking bug this engagement (an unescaped entity that passed typecheck and lint but failed the actual build) — it is not redundant with the first two.
- **Live Walkthrough** — clicked through as a real user would, in the actual running app, not inferred from reading the code. This is the step that found the Act Now membership-count bug; static checks alone would never have caught it.
- **Edge Case Testing** — the empty state, the single-member institution, the second institution type, the person with no Position yet — not just the happy path.
- **Regression Check** — does anything previously verified still work after this change.

Only after all six pass is work considered complete.

---

## 6. Technical Debt Register

Named honestly, never hidden, never silently resolved without a note here:

| Item | Where | Status |
|---|---|---|
| Identity is entirely in-memory — no persistence, resets on server restart | `os/identity/mock-provider.ts` | Intentional, per explicit instruction not to provision Supabase yet. Resolved when the real provider swap happens. |
| No real authentication — dev-mode email sign-in only | `os/identity/actions.ts`, `app/login` | Intentional, same reason. Clearly labeled in the UI as dev-mode. |
| No shared UI primitives — Button/Field/Card patterns are hand-repeated across Login, Onboarding, Settings, and every People drawer/dialog/side panel | `app/login/LoginForm.tsx`, `app/onboarding/OnboardingForm.tsx`, `app/(workspace)/settings/InviteForm.tsx`, `components/os/AppointHolderCard.tsx`, `components/os/AffiliationsCard.tsx`, `components/os/CapabilitiesCard.tsx`, `components/os/CreatePositionCard.tsx`, `components/os/OffboardButton.tsx`, `components/os/PositionSidePanel.tsx`, `components/os/OrganizationCanvas.tsx` | Named in the Product Readiness Review §7, flagged as a risk before People, still not resolved after M3 *and* M4 both shipped the same hand-repeated pattern. Now the single most overdue item in this register — every milestone since M2 has made the eventual extraction more expensive, not less. |
| People has no Position deletion/archival — a Position created by mistake stays forever | `applications/people/mock-provider.ts`, `applications/people/actions.ts` | `Position.status` still has an unused `"active" \| "archived"` field. M4 gave the Organization Builder a natural home for this (an archive action in the side panel) but it wasn't built — deliberately out of scope to keep M4 to its own completion criteria. Next candidate for "implementation reveals a missing concept," per the founder's Build→Verify→Refine directive, once a real institution actually needs it. |
| Organization canvas has no pan/zoom — large institutions with dozens of Positions will scroll, not zoom out | `components/os/OrganizationCanvas.tsx` | New from M4. Fine at demo scale (a handful of Positions); a real institution's full org chart will need real navigation, not just a bigger scrollbar. |
| Position appointment (profile page) and the canvas's own appoint flow are two separate pickers with the same shape, not one shared component | `components/os/AppointHolderCard.tsx`, `components/os/PositionSidePanel.tsx` | New from M4 — a direct consequence of the shared-UI-primitives gap above, not a new problem, just another instance of it. |
| History is an explicitly-labeled preview, not the frozen Audit Engine | `os/attention/history-store.ts` | Intentional and stated in the file's own header. No Events subscription, no `audit_records` table, no permission layering yet. |
| Current prototype's visual language predates the frozen Visual Design System | `app/globals.css`, `tailwind.config.ts` | The prototype was built before the five-theme system was designed. Needs an alignment pass — at minimum bringing the default in line with the Slate theme's actual specification — before more screens multiply the current ad hoc token set. |
| Nav destinations don't change per person — every member sees the identical sidebar, only the actions inside each page differ | `components/os/Shell.tsx`, `os/navigation/index.ts` | Deliberate, not deferred — the founder's own M5 brief was explicit that the goal is not to hide pages. Real gating now exists (M5) and lives entirely on actions within a page, never on the destination list itself. Revisit only if a real institution's use surfaces a genuine reason a full destination — not an action — should be hidden. |
| Responsibility-granting is founder-only, no delegation | `applications/people/actions.ts` (`updatePositionResponsibilitiesAction`) | New from M5, deliberate — avoids a self-escalation loophole (someone with `organization.manage` granting themselves more) without designing a full delegation model this early. A real institution beyond a handful of people will eventually want to delegate this without routing everything through one founder; named honestly as a real future limit, not solved here. Governance & Responsibility Model v1 §3 names the shape Delegation should eventually take — a time-boxed authority grant, never editing the Position itself — unbuilt. |
| Institution-type terminology (`os/institution/terminology.ts`) only covers nav labels + a handful of placeholder examples, not full per-type copy across every screen | `os/institution/terminology.ts` | New from the coherence pass. Covers the highest-impact spots (nav, onboarding, Position/Affiliation/Capability placeholders); Affiliation appointment-type labels ("Permanent/Acting/Temporary") and a few other smaller strings are still institution-neutral, not yet type-aware. |
| Approval Chains have no automatic, time-based Escalation — only the manual, user-triggered kind (M6) | `applications/work/actions.ts` (`escalateApprovalStepAction`) | New from M6, deliberate — automatic escalation needs a real scheduled-job mechanism nothing in this dev-mode, in-memory, single-request-cycle prototype has (no cron, no background worker). Governance §7's philosophy is fully real; only its time-triggered half waits on real infrastructure. |
| Separation of Duties (same-actor exclusion) is a hardcoded permanent default on every Approval, not the *configurable, off-by-default* rule Governance §6 actually specifies | `applications/work/actions.ts` (`decideApprovalStepAction`) | New from M6, deliberate simplification — the smallest real instance of the principle, not the full one. A tiny institution that genuinely wants its founder to both request and approve something has no way to allow that yet. |
| Approval Chains name a bare Area of Responsibility, not a Policy — Institutional Policy Model v1 has no implementation yet | `applications/work/types.ts` (`ApprovalStep.area`) | New from M6, named honestly at the moment it first matters. "Who approves this" is real (Governance); "what makes this specific request correct" (Institutional Policy Model v1) has nowhere to live yet — a chain step today can't say "and it must also satisfy the Purchasing Policy," only "and Finance must approve it." |
| No Round Robin assignment, no template editor — Task/Approval templates are a fixed, hardcoded shape | `applications/work/mock-provider.ts` | New from M6, deliberate. Named reusable-but-unbuilt in the Shared Engine Layer (Product Foundation §7) and the starter-templates-only discipline (§7) respectively; both real, both correctly deferred past a first working slice. |
| No shared form-input primitive (`Input`/`Select`/`Textarea`) — every drawer across Work, Money, Community, Projects, and now Documents hand-rolls the same field-styling string | `components/os/WorkBoard.tsx`, `components/os/MoneyBoard.tsx`, `components/os/CommunityBoard.tsx`, `components/os/ProjectBoard.tsx`, `components/os/DocumentsBoard.tsx` | Named precisely in the ARUMBU Platform Cohesion Review v1 (28+ occurrences at the time). M10 added a sixth file to the pattern rather than resolving it — the single most overdue item in this register, growing more expensive every milestone it's deferred. |
| `notResponsible()` and same-actor-exclusion logic reimplemented independently per application (Work, Finance, now Community) rather than shared from `engines/authority` | `applications/work/actions.ts`, `applications/finance/actions.ts`, `applications/community/actions.ts` | Named in the Cohesion Review; Community repeats the identical `notResponsible()` string a third time rather than importing a shared helper. No drift yet, but nothing structural prevents it. |
| ~~History's `subjectType`/`subjectId` filter only populated by Community's own call sites~~ — **resolved, Platform Integration Sprint 3.** Extended to Position, Work Item, Transaction, and Asset; every one now renders a real filtered Timeline. | `os/attention/history-store.ts`, `applications/people/actions.ts`, `applications/work/actions.ts`, `applications/finance/actions.ts` | Closed. Only remaining gap: Affiliation and Capability records are recorded to institution-level History but not yet given their own `subjectType` — a smaller, later extension, not urgent. |
| Affiliations and Capabilities are recorded to History (Platform Integration Sprint 3 closed the silence) but not yet given their own `subjectType`/`subjectId`, so they have no per-record Timeline the way Positions now do | `applications/people/actions.ts` (`addAffiliationAction`, `endAffiliationAction`, `grantCapabilityAction`, `revokeCapabilityAction`) | New, honestly named rather than silently left implicit. A real but small gap — these events are visible in the institution's shared History today, just not filterable to one Affiliation or Capability's own view yet. |
| No shared `Drawer`/detail-panel primitive — `WorkBoard.tsx`, `MoneyBoard.tsx`, `CommunityBoard.tsx`, `PositionSidePanel.tsx`, `ProjectBoard.tsx`, and now `DocumentsBoard.tsx` each hand-roll their own backdrop/panel/header/close-button markup | `components/os/WorkBoard.tsx`, `components/os/MoneyBoard.tsx`, `components/os/CommunityBoard.tsx`, `components/os/PositionSidePanel.tsx`, `components/os/ProjectBoard.tsx`, `components/os/DocumentsBoard.tsx` | Named in the Cohesion Review, deliberately not resolved across Sprint 3, M9, or M10 — extracting a shared primitive safely across six already-complex files is judged riskier than the duplication it removes, absent real evidence the duplication itself is causing bugs. Still the second-most overdue item after the missing `Input`/`Select`/`Textarea` primitive. |
| Relationship `type`, Expense `category`, and now Project `stage` are free text with only datalist suggestions, no real per-institution-type Type catalog | `applications/community/types.ts`, `applications/finance/types.ts`, `applications/projects/types.ts` | Deliberate, per the Community Domain Review, Finance & Assets Report, and the M9 brief's own explicit "keep this configurable" instruction — a hardcoded enum would be exactly the "company accounting/company-software assumption" all three warned against. A real Institution Configuration Layer Type catalog (Product Foundation §8) would let an institution manage its own list instead of retyping the same value each time; not built yet anywhere in the platform. |
| Community's Contact `addresses`/`pointsOfContact` are encoded as JSON in a hidden form field rather than true multi-value form fields | `components/os/CommunityBoard.tsx`, `applications/community/actions.ts` | New from M8 — the first list-shaped fields any Create/Edit drawer on the platform has needed. Works correctly and is re-validated server-side, but is a real pattern deviation worth flagging before a second domain copies it uncritically. |
| "Mandatory document missing" (named as a real Attention example in the M10 brief) has no implementation — no institution-configured "required documents" list exists anywhere in the platform | `applications/documents/` (none) | Deliberate, named honestly at the moment it first mattered rather than faked with a hardcoded guess at what's "required." Building this needs a real Institution Configuration Layer concept (Product Foundation §8) that doesn't exist yet; inventing one inside Documents would have been new architecture, which M10 was explicitly told not to introduce. |
| Documents' approval is a single Area decision (`documents.manage`), not a real Approval Chain the way Work's Approvals and Finance's Expenses can eventually be — no per-document-type routing | `applications/documents/actions.ts` (`decideDocumentApprovalAction`) | Deliberate simplification for the smallest complete implementation — the brief asked for one genuine "awaiting approval" state, not a full chain. A real institution may eventually want a Policy document to need two Areas in sequence the way a large Expense might; nothing here blocks that later, but nothing builds it now. |
| Every `ReportChart` renders as one plain horizontal bar regardless of its declared `kind` (bar/line/pie/trend) — no line, pie, or trend visual was actually built | `components/os/ReportsBoard.tsx` (`MiniBarChart`) | New from M11, deliberate — the brief's own "numbers first, charts second, no BI platform" was read as license to build one honest, legible chart shape rather than four thin ones for what a founder needs today. The `kind` field is real in the type and ready for a real renderer per kind later; nothing stores or computes anything that would need to change to add one. |
| Analytics' trend observation only covers one fact (expense totals, Financial Summary) — the brief's other six examples (project delays, inactive members, unfilled positions "increasing," approvals waiting, relationships, documents expiring) are all flat-fact counts today, not real trends | `applications/reports/analytics.ts` | New from M11, named honestly. The mechanism (compare the two most recent saved Reports of a category) is real and generalizes to any category once enough Reports of that kind exist; only Financial Summary's expense comparison was actually wired up as the proof case, given the time this milestone had. Extending it to Project/People/Work summaries is mechanical, not a redesign. |
| A Report's `Report.filters` covers `status`/`type` as free-text inputs in the UI, not a picker scoped to what's actually valid for the chosen category (e.g. a Work Summary's `status` means task status, a Document Summary's means document status, and nothing in the form tells you which) | `components/os/ReportsBoard.tsx` (`CreateReportDrawer`) | New from M11, deliberate — the brief's own filter list is universal across nine very different categories, and a real per-category picker would mean nine different filter forms, which felt like exactly the kind of BI-platform complexity the brief said not to build. Named honestly as a real rough edge a founder could hit (typing a Document status into a Work Summary silently filters to nothing) rather than smoothed over. |
| Mock providers mutate their stored record in place and return that same object reference — any action code that reads a "before" field *after* calling the mutating provider function silently reads the already-new value instead | `applications/*/mock-provider.ts` (pattern, not one file) | Found live during M9: Project's stage-change narration recorded "moved from Blocked to Blocked," and the identical root cause was traced back to M6's Work `setTaskStatusAction`, silently disabling its own "reopened" narration since the feature was first built. Both fixed by capturing the "before" value into a local constant before the mutating call. Named here as a pattern risk, not just two isolated bugs — any future action that needs an old value after a provider mutation is at risk of the same silent failure; a real (non-mock) provider backed by a database would not have this specific failure mode, since a fresh row read is a separate value from an in-memory object reference. |

## 7. Backlog

Nothing here disappears. Items move between sections as priority changes; the item itself stays visible in this document's history.

**Critical**
- Provision RDIOS's real Supabase project and swap the Identity provider (blocks all durability; founder's call on timing).

**High**
- Extract shared UI primitives (`Input`/`Select`/`Textarea` above all) — the single most overdue item in the register; four milestones running have repeated the same hand-built pattern, Community adding a fourth.
- Align the current prototype's visuals with the frozen Visual Design System, starting with Slate.
- Grow the responsibility catalog as Projects/Documents/etc. get built (M9+) — each application's real gated actions, not invented ahead of schedule.
- Extend `listHistoryForSubject` coverage to People/Work/Finance's existing call sites, so every Record type gets a real filtered Timeline, not only Community's.
- Fold the Community Domain Review v1 + Reconsideration v1 into a frozen v2, and decide whether the Universal Record Model v1's checklist gets folded into the Product Foundation directly, the same way the Institution Setup Reconsideration was folded into Setup v2.

**Medium**
- Build the real Audit Engine, replacing today's preview.
- Extend `os/institution/terminology.ts` coverage to appointment-type labels and any remaining institution-neutral strings.
- Add Position archival (the `status: "archived"` field already exists on the type, unused) — a natural fit for the Organization Builder's side panel.
- Replace People's single-select Position picker with a searchable one before institution sizes make it unusable.
- Pan/zoom for the Organization canvas once institutions outgrow a scrollable view.
- A real delegation model for responsibility-granting, once a founder-only bottleneck actually shows up in a real institution — Governance & Responsibility Model v1 §3 already names its shape.
- A first Policy record type (Institutional Policy Model v1) so Approval Chains can eventually name a Policy, not just a bare Area.
- Automatic, time-based Escalation for stuck Approval steps, once real scheduled-job infrastructure exists.
- Separation of Duties as the *configurable* rule Governance §6 actually specifies, replacing M6's hardcoded permanent default.

**Low**
- Implement the remaining four themes (Light, Dark, Forest, Midnight Executive) once Slate is correctly implemented.
- Full accessibility pass — reduced-motion support, touch-target audit.

**Future Vision**
- Tamizhi.
- Developer Platform, Marketplace, Public APIs.
- An "Institution Health" view — named as a natural throughline in Institution Setup v2, never committed to as a build.
- Cross-tenant credential verification — named as a future extension in the People Domain Review.

**Ideas**
- Hash-chained, tamper-evident audit trail for compliance-heavy institutions — named in the Audit Engine Design as a real future need, explicitly not designed yet.
- A UI for a person holding memberships in more than one institution simultaneously.

---

## 8. Build Principles

- **Build one slice completely.** One finished application beats five half-built ones — the founder's own standing rule, restated here as policy.
- **Prefer composition over invention.** If an existing engine or pattern can be generalized, generalize it before writing something new.
- **Reuse frozen engines.** RDE's Work Item Engine, Authorization mechanism, Notifications mechanism, Documents pattern, and Workforce schema are proven — port and generalize, don't redesign from scratch.
- **Never rebuild solved problems.** If RDE already proved a pattern works, that question is closed; implementation time goes to genuinely new problems, not re-litigating settled ones.
- **Never reopen frozen architecture without real implementation evidence.** Preference, elegance, or a better idea in the abstract is not sufficient — only a concrete flaw discovered by building against the frozen document reopens it, per the Architecture Freeze Declaration.
- **Always verify before declaring complete.** All six steps in §5, every time, no exceptions for small changes.
- **RDE is never modified, imported, or depended on at runtime.** Reference only, forever.

---

## 9. Definition of Done

A feature is Done only when all of the following are true simultaneously:

- **It works** — the real functionality, not a stub.
- **It is verified** — all six steps in §5 passed.
- **It is documented** — this roadmap reflects it; any new technical debt is named in §6, not hidden.
- **It follows the Product Philosophy** — it serves either institutional memory or institutional attention; if it serves neither, it doesn't belong, regardless of how easy it was to build.
- **It follows the Experience Principles** — correctly tiered (Act Now / Be Aware / History), calm, never a forced interruption.
- **It follows the Visual Design System** — the shared card, type, and spacing language; no invented one-off style.
- **It introduces no architectural regression** — nothing frozen is quietly reopened or contradicted.

Anything short of all seven is not Done — it's still in progress, however close it looks.

---

## 10. Project Dashboard

```
██████████  Philosophy               Frozen
████████░░  Platform Foundation      Prototype
████████░░  Identity                 Prototype
████████░░  Institutions             Prototype
███████░░░  People                   Prototype
███████░░░  Organization             Prototype
███████░░░  Authority & Permissions  Prototype
███████░░░  Work                     Prototype
██████░░░░  Money                    Prototype
██████░░░░  Customers (Community)    Prototype
░░░░░░░░░░  Projects                 Not Started
░░░░░░░░░░  Documents                Not Started
░░░░░░░░░░  Reports                  Not Started
░░░░░░░░░░  Tamizhi                  Not Started
░░░░░░░░░░  Developer Platform       Not Started
░░░░░░░░░░  Marketplace              Not Started
░░░░░░░░░░  Public APIs              Not Started
```

The bars are qualitative, not measured — they exist only to make the labels easier to scan at a glance, per the founder's own instruction not to invent percentages. Philosophy is full because it's frozen, not because a number reached 100. Platform Foundation, Identity, and Institutions are mostly-but-not-fully filled because they're real, working, and verified in their current mock-backed form — genuinely built, genuinely not yet production infrastructure. Institutions now includes Purpose and a minimal org-shape (M2, done) alongside name and type. People moved up slightly past M3 — the coherence pass and M4's multi-parent schema both landed on it — but still missing archival and a scalable Position picker, both named in the Technical Debt Register. Organization is real and verified (M4/M5, done): drag-to-move, drag-to-connect, multi-parent, cycle-guarded, a real side panel with a Responsible-for section — filled a little more than before now that it's cycle-safe, still missing pan/zoom and archival. Authority & Permissions is real and verified (M5, done, extended by M6): a four-key responsibility catalog gating real actions across two applications now, plus the Approval-Chain and Escalation resolution helpers Work depends on — filled a touch more than before, still growing one key per application by design. Work is real and verified (M6, done): Task and Approval, genuine multi-step Approval Chains, same-actor exclusion, Escalation, a per-person Attention Contract — filled slightly less than Authority because Delegation, a template editor, and Policy-backed chains are all real, named, and still ahead of it. Money is real and verified (M7, done): Financial Accounts, a shared Expense/Income transaction spine, the Asset Registry, real Attention/History contributions — filled slightly less than Work because Money's own Policy-backed approvals are an empty seam, not yet real content. Customers (Community) is real and verified (M8, done): Contact (both kinds), Relationship (all three Directions), a genuine filtered Timeline — filled about the same as Money, for the same class of reason: the mechanism is real and complete for what M8 scoped, with Finance/Work/Projects linking, a real Type catalog, and the shared-primitive extraction all still ahead of it. Everything else is empty because nothing has been written yet, honestly.

---

*Update this document every time a milestone status changes, every time technical debt is found or resolved, every time the backlog moves. This is the first thing opened before work begins each day.*
